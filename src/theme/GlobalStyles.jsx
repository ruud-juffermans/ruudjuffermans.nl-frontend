import { CssBaseline, GlobalStyles as MuiGlobalStyles } from "@mui/material";

const GlobalStyles = () => {
  return (
    <>
      <CssBaseline />
      <MuiGlobalStyles
        styles={(theme) => ({

        })}
      />
    </>
  );
};

export default GlobalStyles;
